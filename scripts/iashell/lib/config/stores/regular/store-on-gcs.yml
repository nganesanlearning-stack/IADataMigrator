store:
  name: ${store.name}
  type: GCS
  status: online
  bucket: ${bucket.name.gcs}
  storeType: regular
  storageClass: 
  
spaceRootObject:
  name: ${storageEndPoint.name.gcs}
  storageEndPointCredential: ${storageEndPointCredential.name.gcs}

bucket:
  name: ${bucket.name.gcs}
  spaceRootObject: ${storageEndPoint.name.gcs}
  storageClass:
  location: 

# References to the administration resources
storageEndPoint:
  name: ${storageEndPoint.name.gcs}
  configure: use_existing
  
storageEndPointCredential:
  name: ${storageEndPointCredential.name.gcs}
  configure: use_existing
